machine:
  java:
    version: oraclejdk8
  environment:
    TERM: dumb

general:
  artifacts:
    - build/libs

test:
  override:
    - ./gradlew --parallel build

deployment:
  release:
    tag: /[0-9]+(\.[0-9]+)+/
    commands:
      - ./gradlew -i bintrayUpload
